{
  "name": "@discovery-engine/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": { "dev": "wrangler dev", "deploy": "wrangler deploy", "db:migrate:local": "wrangler d1 migrations apply discovery-engine-db --local", "db:migrate:remote": "wrangler d1 migrations apply discovery-engine-db --remote" },
  "devDependencies": { "wrangler": "^4.33.1" }
}
