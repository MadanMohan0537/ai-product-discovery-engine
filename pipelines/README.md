# Discovery pipeline

`discovery.js` implements the zero-dependency baseline: validation, normalized hashed embeddings, incremental cosine clustering, problem/request signals, transparent opportunity scoring, experiment prompts, and evidence retrieval. Hashed lexical vectors are not semantic language models; optional embedding adapters should be promoted only after benchmark improvement.
