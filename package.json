{
  "name": "ai-product-discovery-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["backend"],
  "scripts": {
    "build": "node scripts/build-web.js",
    "predev": "npm run build",
    "dev": "npm --prefix backend run dev",
    "predeploy": "npm run build",
    "deploy": "npm --prefix backend run deploy",
    "test": "node --test tests/*.test.js",
    "check": "npm run build && node --check pipelines/discovery.js && node --check connectors/csv.js && node --check backend/src/worker.js && node --check apps/web/app.js && node --check apps/web/discovery.js && node --test tests/*.test.js",
    "evaluate": "node evaluation/evaluate.js"
  }
}
