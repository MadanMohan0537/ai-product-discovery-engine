# Evidence-grounded agents

The current “agent” is deliberately deterministic. It converts a scored opportunity into a brief while carrying source evidence IDs. Future generative summaries must cite only retrieved evidence, expose uncertainty, and be evaluated for unsupported claims before they replace this baseline.
