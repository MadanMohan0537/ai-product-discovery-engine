# Applications

`web/` is the zero-build, local-first opportunity dashboard served by the Cloudflare Worker. It imports the same deterministic discovery pipeline as the API. A future framework migration should preserve this shared contract and must not make paid inference mandatory.
